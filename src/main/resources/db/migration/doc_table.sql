create table doc_table(
	FileId int,
	 primary key(FileId),
     name varchar(20),
	CreationDate varchar(20),
	 description varchar(20),
	 Size  int,
	 ParentFolderId int,
	 subject varchar(20),
	 format varchar(20),
	 title varchar(20),
	 
)